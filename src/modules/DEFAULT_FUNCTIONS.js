module.exports = (function () {
  // :: CONSTANTS
  'use strict';
  
  var DEFAULT_FUNCTIONS = {
    long_millis: {
      name: 'millis',
      type: 'long'
    },
    pinRead: {
      name: 'millis',
      type: 'long'
    }
  };

  return DEFAULT_FUNCTIONS;

})();